const NavBar = () => {
  return (
    <header
      id="navbar"
      className="navbar col-span-12 h-[3.75rem] px-4 flex items-center bg-primary text-primary-content"
    >
      <button className="btn btn-ghost text-xl">NavBar</button>
    </header>
  );
};

export default NavBar;
